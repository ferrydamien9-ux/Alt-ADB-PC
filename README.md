# 🛠️ Alt-ADB (PC Version)
Part of the **debugHELPER** suite.

A lightweight Python utility to manage Android devices via ADB without the overhead of heavy IDEs.

## 🚀 How to Run
1. Install [Python](https://python.org).
2. Install dependencies: `pip install -r requirements.txt`
3. Run the tool: `python main.py`

## 📡 Requirements
* USB Debugging must be enabled on your phone.
* ADB Platform tools should be installed on your PC.
